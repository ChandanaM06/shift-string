Shift String Characters

This Python script accepts a string (letters only) and a number, then returns the shifted string.
 Requirements
- Python 3.x


 How to Run:
1. Open terminal/command prompt
2. Navigate to this project folder
3. Run the script:
```bash
python shift_string.py
